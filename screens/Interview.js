import React from 'react';
import { Text, View } from 'react-native';

const Interview = () => {
  return (
    <View
      style={{
        flex: 1,
        backgroundColor: '#27323A',
        color: '#fff',
        textAlign: 'center',
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text style={{ color: '#fff', fontSize: 20 }}>Interview Page</Text>
    </View>
  );
};

export default Interview;
